spring:
  application:
    name : algoSolve
  profiles:
    include: token
  jpa:
    open-in-view: false
springdoc:
  swagger-ui:
    groups-order: ASC
    tags-sorter: alpha
    operations-sorter: method
